-- Vagrant Rim — game configuration.
-- Edited freely without recompiling; loaded at startup by src/config.
return {
    window = {
        width = 1280,
        height = 720,
        title = "Vagrant Rim",
        target_fps = 60,
    },
}
